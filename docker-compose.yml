services:
  backend:
    build: kvspb_time_ticket/
    ports:
      - "8000:8000"

    volumes:
      - db:/usr/src/app/kvspb_time_ticket/db.sqlite3

  bot:
    env_file: "bot.env"
    build: kvspb_hr_bot_main/
    depends_on:
      - backend

volumes:
  db: